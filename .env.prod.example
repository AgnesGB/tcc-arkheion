# Arquivo de ambiente para produção
# NUNCA commitar este arquivo com valores reais!

# Django
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION-$(openssl rand -base64 32)
DEBUG=False

# Database
POSTGRES_DB=arkheion_prod
POSTGRES_USER=arkheion_user
POSTGRES_PASSWORD=CHANGE-THIS-strong-password-123

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1,100.28.223.108

# CSRF Origins (separados por vírgula)
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,http://100.28.223.108

# CORS Origins (separados por vírgula)
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,http://100.28.223.108

# SSL/Domain
DOMAIN_NAME=your-domain.com

# AWS (se usando S3, SES, etc.)
AWS_ACCESS_KEY_ID=your-aws-key
AWS_SECRET_ACCESS_KEY=your-aws-secret
AWS_STORAGE_BUCKET_NAME=your-s3-bucket